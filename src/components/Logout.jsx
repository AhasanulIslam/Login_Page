import React from 'react'

const Logout = () => {
  return (
    <div>(
        <div className="welcome">
          <h2>
            Welcome, <span>{}</span>
          </h2>
          <button>Logout</button>
        </div>
      )</div>
  )
}

export default Logout